# Push-to-Talk Python Dependencies
# Install with: pip install -r requirements.txt

# Speech recognition
faster-whisper

# Keyboard listener
pynput

# OpenAI API (for TTS and Realtime)
openai

# WebSockets for Realtime API
websockets

# Local TTS
piper-tts
pathvalidate

# Audio processing helpers (optional, for noise detection)
numpy

# Streaming STT (Deepgram Nova-3, pin below v6 to avoid breaking API changes)
deepgram-sdk>=5.3,<6.0

# Audio capture (PulseAudio Simple API via PipeWire compat)
pasimple

# Audio playback (callback mode)
pyaudio>=0.2.14

# Silero VAD inference (for barge-in detection)
onnxruntime>=1.18

# Semantic classification fallback (model2vec embeddings)
model2vec

# Sentence boundary detection
pysbd
